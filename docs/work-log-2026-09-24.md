# Work Log — 2026-09-24

## Khắc phục lỗi build và khởi chạy Web (Port 8081)

### 1. Nguyên nhân lỗi khi chạy `flutter run -d chrome --web-port 8081`
- `lib/features/profile/presentation/widgets/closy_wallet_card.dart`: Chứa import `topup_bottom_sheet.dart` (file đã được xóa/ngưng sử dụng trong luồng web payment redirect của spec 009). Gây ra lỗi biên dịch: `Target of URI doesn't exist: 'topup_bottom_sheet.dart'`.
- `lib/core/router/app_router.dart`: Chứa import dư thừa không sử dụng `payment_result_screen.dart` (warning linter).

### 2. Các chỉnh sửa thực hiện
- [x] [closy_wallet_card.dart](file:///d:/Project/smart-wardrobe/smart-wardrobe-mobile/lib/features/profile/presentation/widgets/closy_wallet_card.dart): Xóa dòng import `topup_bottom_sheet.dart`.
- [x] [app_router.dart](file:///d:/Project/smart-wardrobe/smart-wardrobe-mobile/lib/core/router/app_router.dart): Xóa unused import `payment_result_screen.dart`.

### 3. Kiểm thử & Kết quả
- `flutter analyze`: **0 issues** (No issues found).
- Khởi chạy `flutter run -d chrome --web-port 8081`: Ứng dụng đã compile thành công, Chrome đã mở và kết nối DevTools / Dart VM service.

## Tách nền logo trang `/login` (`assets/images/logo-full.png`)

### 1. Vấn đề
- Ảnh logo `assets/images/logo-full.png` có nền màu trắng/nhiễu viền mờ khiến khi hiển thị trên trang đăng nhập (`AppColors.background` = `#FAFAFA` / `#FAF8F5`) bị lộ khung viền vuông.

### 2. Các chỉnh sửa thực hiện
- [x] [logo-full.png](file:///d:/Project/smart-wardrobe/smart-wardrobe-mobile/assets/images/logo-full.png): Tách nền toàn bộ các vùng trắng/canvas ngoài và khoảng trống (negative space), xử lý de-haloing và anti-aliasing mượt mà quanh nét móc áo chữ C và tủ đồ nâu.
- [x] Kiểm tra hiển thị composite trên nền Quiet Luxury: các nét sắc nét, không viền trắng giả (halo), kênh alpha trong suốt đạt 100% độ sạch.

### 3. Kiểm thử
- `flutter analyze`: **0 issues** (No issues found).

## Fix test payment khớp flow web-redirect (009)
- `test/payment_result_test.dart`: test cũ kỳ vọng deep-link map về `/profile/payment/result` nên fail (`Actual: '/profile/subscription'`); đã sửa kỳ vọng thành `equals('/profile/subscription')`, không mở checkout.
- Kiểm thử: `flutter test test/payment_result_test.dart` — 5/5 pass; `flutter analyze` — 0 issues.
- Còn lại 2 file `auth_integration_test.dart` / `outfit_integration_test.dart` fail load do `http://[IP_ADDRESS]/api/v1` khi `dotenv` chưa init trong test — pre-existing từ spec 009, không chặn build AAB.

## Rebuild bản release Play sau update code (spec 008 T013)
- Keystore + `android/key.properties` còn nguyên (không tạo lại).
- Lệnh build (API prod `https://api.closy.hycat.online/api/v1`, Cloudinary `dzvwkngxu`, `ENABLE_PAID_FEATURES=false`):
  - `flutter build appbundle --release --no-tree-shake-icons ...` → `build/app/outputs/bundle/release/app-release.aab` (58.1MB) ✅
  - `flutter build apk --release --no-tree-shake-icons ...` → `build/app/outputs/flutter-apk/app-release.apk` (60.1MB) ✅
- Chưa làm: smoke test APK trên máy thật + upload AAB lên Play Console (T014–T016).

## Internal Testing rollout (spec 008 T016–T017)
- Upload `app-release.aab` 1 (1.0.0) lên Internal testing ✅ — `Available to internal testers`, hỗ trợ 19,195 thiết bị.
- Tạo email list tester + link opt-in; user xác nhận đã Become tester, cài từ Play và mở app được.
- Tên app tạm `com.smartwardrobe.smart_wardrobe (unreviewed)` — bình thường trước review.
- Tiếp theo: smoke test bản cài từ Play (QS-001) → hoàn tất `Finish setting up your app` → Closed testing 12 × 14 ngày.

## Route Chính sách bảo mật mobile + web
- Mobile (mới): `lib/features/profile/presentation/privacy_policy_screen.dart` (Quiet Luxury, nội dung từ `docs/privacy-policy.md`); route `/profile/privacy` trong `lib/core/router/app_router.dart` — KHÔNG gate paid (mở được ở bản Play); menu Hồ sơ luôn hiện mục này.
- Web (repo FE, mới): `smart-wardrobe-fe/src/app/privacy/page.tsx` — trang công khai `/privacy`, cùng nội dung, `tsc --noEmit` sạch.
- Email hỗ trợ tạm dùng `support@closy.hycat.online` ở cả 2 nơi — cần user chốt trùng Store listing.
- Verify: `flutter analyze` 0 issues.
- Lưu ý: AAB 1 (1.0.0) đã upload CHƯA có màn này — màn mobile sẽ theo AAB versionCode tiếp theo; URL privacy dùng cho Console phải là web đã deploy (`https://closy.hycat.online/privacy`).

