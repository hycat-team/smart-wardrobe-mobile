# Work Log — 2026-09-19

Nhật ký công việc spec-driven (specify → clarify → plan → tasks → analyze → implement)
cho 2 feature: deploy Google Play (008) và màn hình Thống kê (009).

## 1. Spec 008 — Google Play Deploy Setup (`specs/008-google-play-deploy-setup/`)

- `/speckit-specify` từ yêu cầu "lên plan setup deploy lên CH play" + tài liệu
  `docs/Flutter_to_Google_Play_Deployment_Guide.md`.
  Kết quả: 3 user story (P1 Internal Testing, P2 store listing & khai báo,
  P3 Closed Testing → Production), 20 FR, 8 SC.
- `/speckit-clarify` — chốt 4 điểm: tài khoản Personal mới (bắt buộc 12 tester
  × 14 ngày), đi trọn tới Production, bản Play đầu tiên ẩn trả phí
  (miễn phí), API production dùng HTTPS thật + tắt cleartext.
- `/speckit-plan` — plan + `research.md` (R1–R13), `data-model.md`,
  `contracts/release-config.md`, `contracts/play-listing.md`, `quickstart.md`.
- `/speckit-tasks` — 36 task (Setup 3, Foundational 3, US1 12, US2 8, US3 5, Polish 5).
- `/speckit-analyze` — 97% coverage; phát hiện HIGH: mâu thuẫn deep-link trong
  checklist test (FR-007) dù deep-link thanh toán bị ẩn. Đã sửa: bỏ deep-link
  khỏi FR-007/SC-002/T014/QS-001; gộp FR-011/FR-025; T007 thêm backup keystore;
  T014/QS-001 thêm kiểm tra hạn mức miễn phí.
- Xác minh chính sách Google Play (tìm kiếm tài liệu chính thức): hàng hóa số
  (subscription, ví) bắt buộc Google Play Billing; PayOS ngoài luồng sẽ bị
  từ chối/gỡ; Việt Nam không thuộc diện alternative billing. Quyết định ẩn
  trả phí ở bản đầu là đúng.
- `/speckit-implement` đợt 1 (phần code) — **15/36 task xong**:
  - T001–T006: toolchain (JDK `Android Studio\jbr\bin\keytool.exe`),
    `docs/Release_Play_Checklist.md`, `docs/privacy-policy.md` (nháp),
    `.gitignore` chặn `key.properties`/`*.jks`/`*.keystore`,
    `lib/core/config/release_flags.dart`, chốt ID
    `com.smartwardrobe.smart_wardrobe` + `1.0.0+1`.
  - T009–T012: Gradle signing Kotlin DSL (release = upload key, không fallback
    debug; debug build vẫn chạy — đã kiểm chứng `flutter build apk --debug`
    thành công), fail-fast `API_BASE_URL` ở release, bỏ cleartext ở manifest
    chính (chỉ giữ ở debug), gate ẩn Ví/Gói hội viên + chặn 4 route
    + bỏ qua deep-link PayOS khi cờ tắt.
  - T018/T032–T034/T036: `flutter analyze` sạch; guide thêm phụ lục giá trị
    riêng; `git check-ignore` xác nhận secrets bị chặn.
- **Còn lại (cần user)**: T007 tạo keystore + backup, T008 `key.properties`,
  URL API production + Cloudinary name → build AAB (T013); smoke test máy thật
  (T014); các bước Play Console + chờ 14 ngày (T015–T031, T035).
- Lưu ý: `flutter run` mặc định ẩn Ví/Gói hội viên; dev test thanh toán thì thêm
  `--dart-define=ENABLE_PAID_FEATURES=true`.

## 2. Spec 009 — Statistics Screen (`specs/009-add-stats-screen/`)

- `/speckit-specify` từ yêu cầu "thêm màn hình thống kê". Phát hiện app đã có
  màn "Thống kê tủ đồ" (`/wardrobe/insights`) nên chốt phạm vi qua 2 câu hỏi:
  màn mới riêng (giữ nguyên màn cũ), v1 gồm đủ 4 nhóm chỉ số, loại trừ chi tiêu.
  Kết quả: 3 user story (P1 tổng quan & sử dụng, P2 giá trị & cost-per-wear,
  P3 outfit & xu hướng), 13→14 FR, 6 SC.
- `/speckit-clarify` — chốt 3 điểm: nguồn số lần mặc/lịch sử mặc chưa xác minh
  (FR-014 bắt buộc kiểm tra API trước), điểm vào từ trang Hồ sơ, xu hướng theo
  tháng 6 tháng gần nhất.
- `/speckit-plan` — plan + `research.md` (R1–R10), `data-model.md`,
  `contracts/stats-data.md`, `quickstart.md` (QS-000→005).
- `/speckit-tasks` — 21 task (Setup 1, Foundational 2, US1 7, US2 4, US3 3, Polish 4).
- `/speckit-analyze` — 97% coverage; sửa I3 (FR-013 vs FR-014), I1/I2, thêm
  coverage FR-009/FR-012 (T022/T023), ma trận SC-003 (QS-006), bỏ `[P]` trùng file.
- `/speckit-implement` — **23/23 task xong, QS pass hết trên máy thật (user xác nhận)**:
  - T001: xác minh trên repo backend — `wearCount` bị **hardcode 0**
    (`item_uc_read.go:432`), không có endpoint lịch sử mặc → fallback FR-014
    hoạt động (ẩn cost-per-wear/xu hướng kèm ghi chú). Ghi vào `research.md`.
  - T002–T009: `wardrobe_stats_models.dart`, `stats_bar_chart.dart`
    (CustomPainter, không thêm dep), `getWardrobeStatistics()` gộp 3 endpoint,
    `wardrobe_stats_provider.dart`, `wardrobe_statistics_screen.dart`,
    route `/wardrobe/statistics`, menu Hồ sơ "Thống kê chi tiết".
  - `flutter analyze` sạch; `flutter test`: 15 pass, 2 integration test
    load-fail **đã chứng minh pre-existing** (stash sạch code vẫn fail —
    thiếu `.env`, `Uri.parse('http://[IP_ADDRESS]...')`), không do 009 gây ra.

## 3. File thay đổi trong phiên

- Sửa: `.gitignore`, `android/app/build.gradle.kts`,
  `android/app/src/{main,debug}/AndroidManifest.xml`,
  `lib/core/constants/app_constants.dart`,
  `lib/core/deeplink/payment_deeplink_handler.dart`,
  `lib/core/router/app_router.dart`,
  `lib/features/profile/presentation/profile_screen.dart`,
  `lib/features/profile/presentation/widgets/closy_wallet_card.dart`,
  `lib/features/wardrobe/data/wardrobe_repository.dart`,
  `docs/Flutter_to_Google_Play_Deployment_Guide.md` (thêm phụ lục).
- Mới: `docs/Release_Play_Checklist.md`, `docs/privacy-policy.md`,
  `lib/core/config/release_flags.dart`,
  `lib/features/wardrobe/models/wardrobe_stats_models.dart`,
  `lib/features/wardrobe/presentation/wardrobe_statistics_screen.dart`,
  `lib/features/wardrobe/providers/wardrobe_stats_provider.dart`,
  `lib/shared/widgets/stats_bar_chart.dart`,
  `specs/008-google-play-deploy-setup/`, `specs/009-add-stats-screen/`,
  file work-log này.
- Chưa commit (chờ user quyết định).
